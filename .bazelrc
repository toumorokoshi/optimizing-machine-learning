# Enforce modern C++ standards
build --cxxopt=-std=c++17

# Show verbose test output on failure
test --test_output=errors

# Use HTML and colorful outputs
common --color=yes

# Autoload rules_cc for legacy built-in cc rules compatibility in Bazel 9+
common --incompatible_autoload_externally=+@rules_cc
